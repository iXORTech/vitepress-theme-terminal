---
title:
  en: "A Draft"
  zh-Hans: "草稿"
---

# A Draft

This folder's `explorer.json` sets `showInExplorer: false` (ARCH-002), so the
whole `drafts/` folder — including this page — is pruned from the explorer
tree while every page in it still builds and remains reachable by URL.

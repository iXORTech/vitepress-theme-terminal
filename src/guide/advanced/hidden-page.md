---
title:
  en: "Hidden Page"
  zh-Hans: "隐藏页面"
showInExplorer: false
---

# Hidden Page

This page sets `showInExplorer: false` in its frontmatter (ARCH-002), so the
auto-discovered explorer does not list it — but the page itself still builds
and stays reachable at its URL, like this one: `/guide/advanced/hidden-page`.

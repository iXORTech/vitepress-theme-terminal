# Series configuration (POST-002, content-architecture.md §5). All fields are
# optional: `icon` is a Font Awesome or Nerd Font `nf-*` class; `title` and
# `description` are LocalizableText (a plain string or a per-language map);
# `order` positions the series on the series index (default 0, smaller =
# higher, ties alphabetical by folder name).
title:
  en: Terminal Internals
  zh-Hans: 终端内幕
description:
  en: A short series on how the theme's shell is built.
  zh-Hans: 关于主题外壳如何构建的简短系列。
icon: nf-fa-terminal
order: 0
